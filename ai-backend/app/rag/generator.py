def generate_response(prompt: str) -> str:
    """Generate NPC dialogue from a completed prompt.

    This placeholder avoids calling Gemini until the API key and final prompt
    contract are ready.
    """
    return (
        "I can sense the shape of your question, but my full reasoning system "
        "has not been connected yet."
    )
